<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CKQKotlin" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
        <item name="colorPrimary">@color/darkBlue</item>
        <item name="textAppearanceButton">@color/white</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:colorBackground">@color/navy</item>
        <item name="android:textCursorDrawable">@null</item>
        <item name="materialButtonStyle">@style/ButtonColor</item>
        <item name="android:progressTint">@color/darkBlue</item>
    </style>

    <style name="ButtonColor" parent="Widget.Material3.Button">
        <item name="android:textColor">@color/white</item>
    </style>
</resources>